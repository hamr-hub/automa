name: Build and Release

on:
  push:
    tags:
      - 'v*.*.*'  # 匹配语义化版本标签,如 v1.29.12

permissions:
  contents: write

jobs:
  build-and-release:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20.11.1'
          cache: 'npm'

      - name: Install dependencies
        run: npm install

      - name: Create getPassKey.js
        run: |
          echo 'export default function() { return "production-key"; }' > src/utils/getPassKey.js

      - name: Build Chrome version
        run: npm run build:prod-chrome

      - name: Build Firefox version
        run: npm run build:prod-firefox

      - name: Get version from tag
        id: get_version
        run: echo "VERSION=${GITHUB_REF#refs/tags/v}" >> $GITHUB_OUTPUT

      - name: Create Release
        id: create_release
        uses: actions/create-release@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tag_name: ${{ github.ref }}
          release_name: Release ${{ github.ref_name }}
          draft: false
          prerelease: false
          body: |
            ## Automa ${{ github.ref_name }}
            
            ### 📦 安装方式
            
            **Chrome/Edge:**
            1. 下载 `automa-chrome-${{ steps.get_version.outputs.VERSION }}.zip`
            2. 解压文件
            3. 打开 `chrome://extensions`
            4. 启用"开发者模式"
            5. 点击"加载已解压的扩展程序"
            6. 选择解压后的文件夹
            
            **Firefox:**
            1. 下载 `automa-firefox-${{ steps.get_version.outputs.VERSION }}.zip`
            2. 打开 `about:addons`
            3. 点击齿轮图标 → "Install Add-on From File"
            4. 选择下载的 zip 文件
            
            ### 🔄 更新内容
            请查看 [Changelog](https://github.com/${{ github.repository }}/blob/main/CHANGELOG.md)

      - name: Upload Chrome Extension
        uses: actions/upload-release-asset@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          upload_url: ${{ steps.create_release.outputs.upload_url }}
          asset_path: ./build/chrome.zip
          asset_name: automa-chrome-${{ steps.get_version.outputs.VERSION }}.zip
          asset_content_type: application/zip

      - name: Upload Firefox Extension
        uses: actions/upload-release-asset@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          upload_url: ${{ steps.create_release.outputs.upload_url }}
          asset_path: ./build/firefox.zip
          asset_name: automa-firefox-${{ steps.get_version.outputs.VERSION }}.zip
          asset_content_type: application/zip

      - name: Build Summary
        run: |
          echo "### ✅ Build Successful" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**Version:** ${{ github.ref_name }}" >> $GITHUB_STEP_SUMMARY
          echo "**Chrome:** automa-chrome-${{ steps.get_version.outputs.VERSION }}.zip" >> $GITHUB_STEP_SUMMARY
          echo "**Firefox:** automa-firefox-${{ steps.get_version.outputs.VERSION }}.zip" >> $GITHUB_STEP_SUMMARY
